<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Professor Profile</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    :root{--blue:#4b6cb7;--bg:#f3f5f9;--card:#fff}
    *{box-sizing:border-box;margin:0;padding:0}
    body{font-family:"Segoe UI",sans-serif;background:var(--bg);color:#222;padding:18px}
    .container{max-width:960px;margin:24px auto}
    .card{background:var(--card);padding:22px;border-radius:12px;box-shadow:0 10px 30px rgba(0,0,0,0.06)}
    .row{display:flex;gap:18px;align-items:center}
    .avatar{
      width:84px;height:84px;border-radius:50%;
      background:var(--blue);
      display:flex;align-items:center;justify-content:center;
      color:#fff;font-weight:800;font-size:32px;flex-shrink:0;
    }
    h1{font-size:24px;margin-bottom:12px}
    .grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}
    label{font-weight:700;font-size:13px;margin-bottom:6px;display:block;color:#333}
    input{width:100%;padding:10px;border-radius:8px;border:1px solid #e6e9ef;font-size:14px}
    input[readonly]{background:#f5f6f8;color:#666}
    .actions{display:flex;gap:10px;justify-content:flex-end;margin-top:18px}
    .btn{padding:8px 14px;border-radius:8px;background:var(--blue);color:#fff;border:none;font-weight:700;cursor:pointer}
    .btn.ghost{background:#777}
    .meta{color:#666;font-size:13px;margin-left:12px}
    .msg{padding:10px;border-radius:8px;margin-top:10px}
    .success{background:#e9f7ee;color:#1f8a3a}
    .error{background:#fdecea;color:#a12a2a}
    @media(max-width:760px){
      .grid{grid-template-columns:1fr}
      .row{flex-direction:column;align-items:flex-start}
    }
  </style>
</head>
<body>

<div class="container">
  <div class="card">
    <div class="row">
      <div class="avatar">
        <%= professor.name ? professor.name[0].toUpperCase() : 'P' %>
      </div>
      <div>
        <h1>Professor Profile</h1>
        <div class="meta">
          <strong><%= professor.name %></strong> — <%= professor.email %>
        </div>
      </div>
    </div>

    <form id="profileForm">
      <div class="grid" style="margin-top:18px">
        <div>
          <label>Name</label>
          <input type="text" value="<%= professor.name %>" readonly>
        </div>

        <div>
          <label>Department</label>
          <input type="text" value="<%= professor.departmentName %>" readonly>
        </div>

        <div>
          <label>Email</label>
          <input type="email" value="<%= professor.email %>" readonly>
        </div>

        <div>
          <label>Phone</label>
          <input type="text" name="phone" value="<%= professor.phone || '' %>">
        </div>

        <div style="grid-column:1/-1">
          <label>Change Password</label>
          <input type="password" name="newPassword" placeholder="New password">
        </div>
      </div>

      <div class="actions">
        <button type="button" class="btn ghost" onclick="location.href='/professor/dashboard'">Cancel</button>
        <button class="btn">Save Changes</button>
      </div>

      <div id="msg" style="display:none"></div>
    </form>
  </div>
</div>

<script>
const form = document.getElementById("profileForm");
const msg = document.getElementById("msg");

form.addEventListener("submit", async (e)=>{
  e.preventDefault();
  msg.style.display="none";

  const data = {
    phone: form.phone.value.trim(),
    newPassword: form.newPassword.value.trim()
  };

  if(data.newPassword && data.newPassword.length < 6){
    msg.style.display="block";
    msg.className="msg error";
    msg.textContent="Password must be at least 6 characters";
    return;
  }

  const res = await fetch("/professor/profile",{
    method:"POST",
    headers:{"Content-Type":"application/json"},
    body:JSON.stringify(data)
  });

  const json = await res.json();
  msg.style.display="block";

  if(res.ok){
    msg.className="msg success";
    msg.textContent=json.message;
    form.newPassword.value="";
  }else{
    msg.className="msg error";
    msg.textContent=json.error;
  }
});
</script>

</body>
</html>
