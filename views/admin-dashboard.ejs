<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Dashboard</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    * { box-sizing: border-box; margin:0; padding:0; }
    html,body { height:100%; }
    body {
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(135deg, #e9f0ff, #f3f5f9);
      color: #222;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ✅ FIXED NAVBAR */
    .navbar {
      background: #4b6cb7;
      height: 64px;
      display: flex;
      justify-content: space-between;
      align-items: center;     /* <-- PERFECT vertical alignment */
      padding: 0 16px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.06);
    }

    .menu-toggle {
      width:44px;
      height:44px;
      display:flex;
      align-items:center;
      justify-content:center;
      cursor:pointer;
      border-radius:8px;
      transition: background .15s;
    }

    .menu-toggle .bars {
      display:flex;
      flex-direction:column;
      gap:6px;
    }
    .menu-toggle .bars span {
      width:20px;
      height:2.6px;
      background:white;
      border-radius:2px;
    }

    /* ✅ FIXED: REMOVE ABSOLUTE — USE NORMAL FLEX */
    .navbar .title {
      font-weight: 800;
      color: white;
      font-size: 20px;
      text-align: center;
      margin: 0 auto;   /* <-- centers text */
      white-space: nowrap;
    }

    .navbar .right {
      display:flex;
      align-items:center;
      color:white;
      font-weight:600;
    }

    /* Rest of your styles unchanged */
    .page-wrap { display:flex; gap:20px; padding:20px; flex:1; }
    .sidebar { width:240px; background:white; border-radius:10px; padding:12px; }
    .sidebar a { display:block; padding:12px 14px; background:#f5f6f8; margin-bottom:10px; border-radius:8px; text-align:center; }

    .user-panel { display:flex; justify-content:space-between; padding:10px; background:#fff; border-radius:8px; margin-bottom:12px; }
    .name { font-weight:800; font-size:14px; }
    .role { font-size:12px; }

    .logout-btn { background:#d23c37; color:white; padding:8px; border:none; border-radius:8px; cursor:pointer; }

    .main { flex:1; }
    .container { background:white; padding:28px; border-radius:12px; }
    .container h1 { text-align:center; font-size:34px; }

    .overview { display:grid; grid-template-columns:repeat(auto-fit,minmax(180px,1fr)); gap:22px; }

    .card { padding:20px; border-radius:12px; text-align:center; }
    .departments { background:#e8f2ff; }
    .students    { background:#e9fdf0; }
    .professors  { background:#fff3e7; }
    .hods        { background:#f6ecff; }

    .app-footer { padding:16px; text-align:center; }

    /* Modal CSS unchanged */
    .modal-overlay { position:fixed; inset:0; background:rgba(0,0,0,0.45); display:none; justify-content:center; align-items:center; }
    .modal-box { background:white; padding:20px; border-radius:14px; width:430px; text-align:center; }
  </style>
</head>

<body data-admin="<%= adminName %>">

  <!-- NAVBAR -->
  <div class="navbar">
      <div id="menuBtn" class="menu-toggle">
        <div class="bars"><span></span><span></span><span></span></div>
      </div>

      <div class="title">University Admin Dashboard</div>

      <div class="right">
        <div id="greeting">Hello, <%= adminName %></div>
      </div>
  </div>

  <div class="page-wrap">
    <nav class="sidebar" id="sidebar">
      <div class="user-panel">
        <div>
          <div id="sidebarUserName" class="name"><%= adminName %></div>
          <div id="sidebarUserRole" class="role">Admin</div>
        </div>
        <button id="sidebarLogoutBtn" class="logout-btn">Logout</button>
      </div>

      <a href="/admin/dashboard" class="active">Dashboard Overview</a>
      <a href="/admin/departments">Departments</a>
      <a href="/admin/users">Users</a>
    </nav>

    <main class="main" id="mainContent">
      <div class="container">
        <h1>Dashboard Overview</h1>

        <div class="overview">
          <div class="card departments"><h3>Total Departments</h3><p><%= totalDepartments %></p></div>
          <div class="card students"><h3>Total Students</h3><p><%= totalStudents %></p></div>
          <div class="card professors"><h3>Total Professors</h3><p><%= totalProfessors %></p></div>
          <div class="card hods"><h3>Total HODs</h3><p><%= totalHODs %></p></div>
        </div>
      </div>
    </main>
  </div>

  <footer class="app-footer">© <%= new Date().getFullYear() %> Your University</footer>

  <!-- Modal -->
  <div id="logoutModal" class="modal-overlay">
    <div class="modal-box">
      <h3>Are you sure you want to logout?</h3>
      <p>This will end your session.</p>
      <button id="cancelLogout" class="btn-no">No</button>
      <button id="confirmLogout" class="btn-yes">Yes</button>
    </div>
  </div>

  <script>
    // Greeting
    document.getElementById("greeting").innerText = "Hello, <%= adminName %>";
  </script>

</body>
</html>
